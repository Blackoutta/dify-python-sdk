## 1. Define skill-doc structure

- [x] 1.1 Create the `cli-skills/` file layout for the main `skills.md` index and the detailed command-family reference files
- [x] 1.2 Decide the command-family grouping and file naming scheme that maps cleanly to the existing CLI capabilities

## 2. Author the main index

- [x] 2.1 Write `cli-skills/skills.md` as the primary AI-agent entrypoint with quick command-family routing
- [x] 2.2 Add best-practice guidance to `cli-skills/skills.md` for configuration resolution, JSON output, metadata inspection, and structured input handling

## 3. Author detailed command references

- [x] 3.1 Create the detailed command-family files under `cli-skills/` with command purposes, notable parameters, and required identifiers
- [x] 3.2 Add usage notes to each detailed file for payload shape, response mode, and command-specific caveats drawn from the CLI specs

## 4. Validate coverage and consistency

- [x] 4.1 Cross-check the `cli-skills/` files against the active CLI OpenSpec capabilities to confirm command-family coverage and link accuracy
- [x] 4.2 Review the skill docs for concise agent-friendly wording so the main index stays lightweight while detail files remain actionable
